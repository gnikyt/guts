<div class="row">
  <div class="small-12 columns">
    <h1>Content Types</h1>
    <%= link_to('New', new_type_path, class: 'button') if policy([:guts, :type]).create? %>
  </div>
</div>

<% if notice %>
  <div class="row">
    <div class="small-12 columns">
      <div class="callout success notice">
        <p><%= notice %></p>
      </div>
    </div>
  </div>
<% end %>

<div class="row">
  <div class="small-12 columns">
    <% if @types.size > 0 %>
      <table>
        <tbody>
          <%= render partial: 'type_row', collection: @types %>
        </tbody>
      </table>
    <% else %>
      <p>No content types have been added.</p>
    <% end %>
  </div>
</div>

<div class="row">
  <div class="small-12 columns">
    <h1>New Metafield<% if @object.try(:title) or @object.try(:name) %> <span class="sub_title">for <%= sub_title_for @object %>: <%= @object.try(:title) || @object.try(:name) %></span><% end %></h1>
  </div>
</div>

<div class="row">
  <%= render "form" %>
</div>
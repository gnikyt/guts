<% if track.object.nil? %>
  User <u>who no longer exists (#<%= track.object_id.to_s %>)</u>, was updated
<% else %>
  User <u><%= track.object.name %></u> was updated
  <%= render partial: "guts/trackers/actions/params", locals: {track: track} %>
<% end %>
<% if track.object.nil? %>
  Navigation <u>that no longer exists (#<%= track.object_id.to_s %>)</u>, was updated
<% else %>
  Navigation <u><%= track.object.title %></u> was updated
  <%= render partial: "guts/trackers/actions/params", locals: {track: track} %>
<% end %>
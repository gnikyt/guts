<% if track.object.nil? %>
  Site <u>which no longer exists (#<%= track.object_id.to_s %>)</u>, was updated
<% else %>
  Site <u><%= track.object.name %></u> was updated
  <%= render partial: "guts/trackers/actions/params", locals: {track: track} %>
<% end %>